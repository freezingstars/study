# 基础
## ELF文件概述
![[ELF.png]]
ELF文件头表(ELF header)
	- 记录了ELF文件组织结构

程序头表/段表(Program header table)
	- 告诉系统**如何创建进程**
	- 生成进程的**可执行文件必须拥有此结构**
	- 重定位文件非必须

节头表(Section header table)
	- 记录了ELF文件的**节区信息**
	- 用于**链接的目标文件必须拥有此结构**
	- 其他类型目标文件不一定需要

**段**视图：程序**加载到内存**中成为进程后进行读、写、执行权限划分的视图
**节**视图：ELF文件存放在**磁盘中**进行不同功能区域划分的视图

代码（text）段：**代码和只读数据**
text：编译生成的代码主体
rodata：只读的初始化数据，通常是**常量与常变量**
hash：初始化的**全局变量**和**静态变量**
plt：过程链接表
rel.got：重定位节区

数据（data）段：**可读写数据**
data：可读写初始化的**全局变量**和**静态变量**
got：全局偏移表
got.plt：存储链接库函数的最终地址

## 内存
**段视图**用于进程的内存区域的**rwx权限划分**
**节视图**用于**ELF文件**编译链接时与在磁盘上存储时的**文件结构**的组织
执行文件时，需要将程序从外存加载到内存中
不同的节会被合并为一整**段**
![](ELF2.png)
所以
具有**RW**权限的为
	数据段(.**data**)
	未初始化的数据段(.**bss**)
	全局偏移表(.**got**)，过程链接表(.**plt**)
具有**RX**权限的为
	只读数据段(.**rodata**)
	代码段(.**text**)
	初始化函数(.**init**)
	ELF头(**ELF** **Header**)
*got里面存放的是地址，不可执行，.plt里存放的是代码，不可修改，可执行*

![](ELF3.png)
不同的节section由于权限相同，会被合并为一个段segment
通过Linker合并

虚拟内存**用户空间**每个进程一份
虚拟内存**内核空间**所有进程共享一份
虚拟内存mmap段中的**动态链接库**仅在物理内存中装载一份

32位系统下，内核空间仅占1G，64位中占128TB范围
![](Memory.png)
动态存储区大小会随着函数调用和销毁改变
静态存储区在最初大小就已经固定了

![](Memory2.png)
Stack：栈，局部变量、函数调用的内存开销也在此处
shared libraries：共享库文件
Heap：堆，malloc等函数申请的内存空间都在此处
BSS：存放声明但未进行初始化的变量
DATA：存放可读写的数据
TEXT：存放可读和执行的数据和代码

Hello world是常量字符串，所以被分配在代码段中不可修改。**重新赋值修改的是“指针本身的指向”，而不是它指向的内容。**

小端序：低地址存放数据低位，高地址存放数据高位。主要关注的格式
大端序：和小端序相反

## 寄存器
amd64寄存器结构：
- rax: 8Bytes
- eax: 4Bytes
- ax: 2Bytes
- ah/al: 1Byte

RIP：存放当前执行的指令的下一跳地址
RSP：存放当前栈帧的栈顶地址
RBP：存放当前栈帧的栈底地址
RAX：通用寄存器，存放函数返回值

在x86架构中为ESP, EBP和EAX。amd64算是一种超集，可包容x86寄存器。

## 从编译到执行
预处理->编译->汇编->链接->执行
gcc -E -> -S -> -c -> -o

|阶段|做什么|
|---|---|
|预处理（-E）|宏展开、头文件展开、条件编译|
|编译（-S）|语法分析 + 生成IR + **优化**|
|汇编（-c）|转机器码|
|链接|合并目标文件|
file 文件名
LSB小端序，MSB大端序
fork：创建一个新的进程
execve：系统调用，它允许进程用一个全新的程序替换当前运行的程序。这个过程中，旧程序的栈、数据和堆段会被新程序所替换
sys_execve：是 Linux 内核中的一个系统调用，用于执行一个新的程序。它会替换当前进程的程序，使其执行指定的可执行文件。这个过程涉及从用户空间拷贝文件名、参数及环境到内核空间，加载可执行文件，并释放旧进程资源等关键步骤。
ld.so：动态链接程序
`_libc_start_main`：静态和动态都会有的一步
`_init`：初始化步骤
load_elf_binary：使用elf格式加载器，解析elf文件头的同时将数据段和代码段映射到内存中。

Base：基地址
index：索引
scale：度
偏移地址=0+2\*4。Displacement，位移。假设位移为0x10。$0+2*4+0x10=24$ 
偏移：offset

| 名称           | 语境                | 是否固定    | 单位  | 举例                         |
| ------------ | ----------------- | ------- | --- | -------------------------- |
| offset       | 高级语言 / 结构体 / 段寄存器 | 通常固定    | 字节  | `offsetof(struct A, y)`    |
| displacement | 汇编寻址（基址+索引）       | 可以动态或固定 | 字节  | `[ebx + esi*4 + 8]` 中的 `8` |
$Effective Address=Base+(Index×Scale)+Displacement$

- **Base（基址寄存器）**：通常是指向数据块或数组起始位置的寄存器（如 `ebx`、`rbp`）
- **Index（索引寄存器）**：数组或结构体内元素的索引（如 `esi`）
- **Scale（度/乘数）**：元素大小（1、2、4、8）
- **Displacement（常偏移量）**：相对于基址的固定偏移，常数
displacement 可以用来**把基址调整到目标数据段起点**，这样 Base + Index\*Scale 就能方便访问数组/序列任意元素，尤其在结构体或栈帧里间隔了其他内容时非常有用。

在AMD架构中，目的操作数在后；在Intel架构中，目的数在前
MOV的两个操作数不能同时为内存，LEA可以将源操作数的**有效地址**传递给寄存器
AMD架构中立即数4的表示为`$4`

|后缀|全称/意义|位数|示例|
|---|---|---|---|
|`movb`|Move Byte|8 位|`movb al, [ebx]` → 将内存 1 字节复制到 AL|
|`movw`|Move Word|16 位|`movw ax, [bx]` → 内存 2 字节复制到 AX|
|`movl`|Move Long|32 位|`movl eax, [ebx]` → 内存 4 字节复制到 EAX|
|`movq`|Move Quad|64 位|`movq rax, [rbx]` → 内存 8 字节复制到 RAX|
